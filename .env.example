# exchangerate-api.com key for the currency-converter card's live rates.
# Server-only on purpose — see src/app/api/rates/route.ts for why it must not
# be NEXT_PUBLIC_*. Without it the card falls back to its built-in rate table
# and says so in the footer, so local dev works fine unset.
EXCHANGERATE_API_KEY=
