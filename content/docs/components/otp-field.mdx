---
title: "Otp Field"
description: "Otp Field component built on Base UI, styled with sc1m tokens."
---

import { story } from "@/components/ui/otp-field/otp-field.story";

## Preview

A representative composition. Open it in Storybook to vary the props.

<story.WithControl />

## Installation

```bash
npx shadcn@latest add @sc1m/otp-field
```

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts` plus `@base-ui/react`.

## Usage

```tsx
import { OTPField, OTPFieldInput } from "@/components/ui/otp-field";
```

## Exports

- `OTPField`
- `OTPFieldInput`

## Source

[`src/components/ui/otp-field/otp-field.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/otp-field/otp-field.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
