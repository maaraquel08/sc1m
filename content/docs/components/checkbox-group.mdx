---
title: "Checkbox Group"
description: "Checkbox Group component built on Base UI, styled with sc1m tokens."
---

import { Demo as ComponentDemo } from "@/components/ui/checkbox-group/checkbox-group.demo";
import { propColumns } from "@/components/site/docs-page";

<Chips items={[{ label: "Stable", tone: "success" }, { label: "Base UI" }]} />

## Live preview

<Preview>
  <ComponentDemo />
</Preview>

## Installation

<PMCommand exec="shadcn@latest add @sc1m/checkbox-group" />

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts` plus `@base-ui/react`. For the manual route — prerequisites,
`components.json`, and app-level setup — see [Installation](/docs/installation).

## Usage

```tsx lineNumbers
import { CheckboxGroup } from "@/components/ui/checkbox-group";
```

## Example

The preview above, with its source. This is the demo module verbatim, so
the code and the thing it renders cannot drift apart.

<Demo>
<DemoPreview>
  <ComponentDemo />
</DemoPreview>
<DemoCode>

```tsx lineNumbers
<CheckboxGroup aria-label="Fruits">
  {fruits.map((fruit) => (
    <label key={fruit} className="flex items-center gap-2 text-sm">
      <Checkbox name={fruit} />
      {fruit}
    </label>
  ))}
</CheckboxGroup>
```

</DemoCode>
</Demo>

## API reference
Each part forwards every prop of its Base UI counterpart; the tables list
the ones declared on the part itself. See
[Base UI's reference](https://base-ui.com/react/components/checkbox-group)
for the inherited element props.

### CheckboxGroup

<SpecTable
  columns={propColumns}
  widths="1.1fr 1.3fr 0.7fr 2fr"
  rows={[
    ["value", "string[]", "—", "Names of the checkboxes in the group that should be ticked."],
    ["defaultValue", "string[]", "—", "Names of the checkboxes in the group that should be initially ticked."],
    ["onValueChange", "((value: string[], eventDetails: CheckboxGroupChangeEventDetails) => void)", "—", "Event handler called when a checkbox in the group is ticked or unticked."],
    ["allValues", "string[]", "—", "Names of all checkboxes in the group."],
    ["disabled", "boolean", "false", "Whether the component should ignore user interaction."],
  ]}
/>

## Accessibility

Behaviour, focus management, and ARIA wiring come from [Base UI's Checkbox Group](https://base-ui.com/react/components/checkbox-group) — see its reference for the full keyboard map.

- Disabled parts mute their contents rather than the element, keeping the focus ring at full strength where the part stays focusable.

## Source

[`src/components/ui/checkbox-group/checkbox-group.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/checkbox-group/checkbox-group.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
