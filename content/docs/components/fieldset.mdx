---
title: "Fieldset"
description: "Fieldset component built on Base UI, styled with sc1m tokens."
---

import { story } from "@/components/ui/fieldset/fieldset.story";

## Preview

A representative composition. Open it in Storybook to vary the props.

<story.WithControl />

## Installation

```bash
npx shadcn@latest add @sc1m/fieldset
```

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts` plus `@base-ui/react`.

## Usage

```tsx
import { Fieldset, FieldsetLegend } from "@/components/ui/fieldset";
```

## Exports

- `Fieldset`
- `FieldsetLegend`

## Source

[`src/components/ui/fieldset/fieldset.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/fieldset/fieldset.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
