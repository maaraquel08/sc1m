---
title: "Button"
description: "Button component built on Base UI, styled with sc1m tokens."
---

import { Demo as ComponentDemo, ButtonStates } from "@/components/ui/button/button.demo";
import { tokenColumns, propColumns } from "@/components/site/docs-page";

<Chips items={[{ label: "Stable", tone: "accent" }]} />

## Live preview

<Preview>
  <ComponentDemo />
</Preview>

## Installation

<PMCommand exec="shadcn@latest add @sc1m/button" />

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts`. For the manual route — prerequisites,
`components.json`, and app-level setup — see [Installation](/docs/installation).

## Usage

```tsx lineNumbers
import { Button } from "@/components/ui/button";
```

## Example

The preview above, with its source. This is the demo module verbatim, so
the code and the thing it renders cannot drift apart.

<Demo>
<DemoPreview>
  <ComponentDemo />
</DemoPreview>
<DemoCode>

```tsx lineNumbers
<Button>Save changes</Button>
```

</DemoCode>
</Demo>

## States

Every variant shares one focus ring and one disabled treatment; only the hover
step is per-variant.

{/* bg-bg, not the frame's default bg-surface: secondary and ghost hover ARE
    bg-surface, so on a tinted frame those cells would be invisible. */}
<Preview className="bg-bg">
  <ButtonStates />
</Preview>

<SpecTable
  columns={[
    { label: "State", className: "font-mono text-xs text-fg" },
    { label: "What changes", className: "font-mono text-xs text-info" },
    { label: "Notes", className: "text-fg-muted" },
  ]}
  widths="0.8fr 1.6fr 2fr"
  rows={[
    ["hover", "primary → bg-accent-hover · secondary/ghost → bg-surface · danger → opacity-90", "The only per-variant state. Danger shifts opacity rather than hue so the red stays the danger token."],
    ["focus-visible", "outline-2 outline-offset-2 outline-ring", "Shared by all four. Keyboard only — a pointer press never paints a ring."],
    ["disabled", "opacity-50 + pointer-events-none", "Shared by all four. Native disabled, so the button leaves the tab order."],
    ["active", "—", "There is no pressed style. A press reads only through the hover state and the 150ms colour transition."],
  ]}
/>

<Callout title="How this table is rendered">
  Hover and focus cannot be produced from a real pointer in a static page, so
  those two columns force the state with the same utility the component applies
  for it. Default and Disabled are unforced — `disabled` is the real prop.
</Callout>

## Accessibility

- Keyboard focus draws a 2px ring in `--ring`, never removed — only shown on `:focus-visible`, so a pointer press does not paint one.

## Design tokens

The component reads semantic tokens only — no raw colour, radius, or
duration appears in its source. Override the token, not the component.

<SpecTable
  columns={[tokenColumns[0], tokenColumns[2]]}
  widths="1.2fr 1.8fr"
  minWidth={420}
  rows={[
    ["--ring", "Button"],
  ]}
/>

## Source

[`src/components/ui/button/button.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/button/button.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
