---
title: "Number Field"
description: "Number Field component built on Base UI, styled with sc1m tokens."
---

import { story } from "@/components/ui/number-field/number-field.story";

## Preview

A representative composition. Open it in Storybook to vary the props.

<story.WithControl />

## Installation

```bash
npx shadcn@latest add @sc1m/number-field
```

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts` plus `@base-ui/react`.

## Usage

```tsx
import {
  NumberField,
  NumberFieldGroup,
  NumberFieldInput,
  NumberFieldIncrement,
  NumberFieldDecrement,
  NumberFieldScrubArea,
} from "@/components/ui/number-field";
```

## Exports

- `NumberField`
- `NumberFieldGroup`
- `NumberFieldInput`
- `NumberFieldIncrement`
- `NumberFieldDecrement`
- `NumberFieldScrubArea`

## Source

[`src/components/ui/number-field/number-field.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/number-field/number-field.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
