---
title: "Input"
description: "Input component built on Base UI, styled with sc1m tokens."
---

import { Demo as ComponentDemo } from "@/components/ui/input/input.demo";
import { propColumns } from "@/components/site/docs-page";
import { tokenColumns } from "@/components/site/docs-page";

<Chips items={[{ label: "Stable", tone: "accent" }, { label: "Base UI" }]} />

## Live preview

<Preview>
  <ComponentDemo />
</Preview>

## Installation

<PMCommand exec="shadcn@latest add @sc1m/input" />

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts` plus `@base-ui/react`. For the manual route — prerequisites,
`components.json`, and app-level setup — see [Installation](/docs/installation).

## Usage

```tsx lineNumbers
import { Input } from "@/components/ui/input";
```

## Example

The preview above, with its source. This is the demo module verbatim, so
the code and the thing it renders cannot drift apart.

<Demo>
<DemoPreview>
  <ComponentDemo />
</DemoPreview>
<DemoCode>

```tsx lineNumbers
<Input placeholder="Enter text…" />
```

</DemoCode>
</Demo>

## API reference
Each part forwards every prop of its Base UI counterpart; the tables list
the ones declared on the part itself. See
[Base UI's reference](https://base-ui.com/react/components/input)
for the inherited element props.

### Input

<SpecTable
  columns={propColumns}
  widths="1.1fr 1.3fr 0.7fr 2fr"
  rows={[
    ["onValueChange", "((value: string, eventDetails: Input.ChangeEventDetails) => void)", "—", "Callback fired when the value changes."],
    ["defaultValue", "Field.Control.Props['defaultValue']", "—", "The default value of the input."],
    ["value", "React.ComponentProps<'input'>['value']", "—", "The value of the input."],
  ]}
/>

## Accessibility

Behaviour, focus management, and ARIA wiring come from [Base UI's Input](https://base-ui.com/react/components/input) — see its reference for the full keyboard map.

## Design tokens

The component reads semantic tokens only — no raw colour, radius, or
duration appears in its source. Override the token, not the component.

<SpecTable
  columns={[tokenColumns[0], tokenColumns[2]]}
  widths="1.2fr 1.8fr"
  minWidth={420}
  rows={[
    ["--brand-radius-md", "Input"],
    ["--fg", "Input"],
    ["--fg-subtle", "Input"],
    ["--line", "Input"],
    ["--ring", "Input"],
    ["--surface", "Input"],
    ["--surface-raised", "Input"],
  ]}
/>

## Source

[`src/components/ui/input/input.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/input/input.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
