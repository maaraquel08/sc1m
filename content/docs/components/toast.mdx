---
title: "Toast"
description: "Toast component built on Base UI, styled with sc1m tokens."
---

import { story } from "@/components/ui/toast/toast.story";

## Preview

A representative composition. Open it in Storybook to vary the props.

<story.WithControl />

## Installation

```bash
npx shadcn@latest add @sc1m/toast
```

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts` plus `@base-ui/react`.

## Usage

```tsx
import {
  ToastProvider,
  useToastManager,
  createToastManager,
  useToast,
  ToastViewport,
  Toast,
  ToastTitle,
  ToastDescription,
  ToastClose,
} from "@/components/ui/toast";
```

## Exports

- `ToastProvider`
- `useToastManager`
- `createToastManager`
- `useToast`
- `ToastViewport`
- `Toast`
- `ToastTitle`
- `ToastDescription`
- `ToastClose`

## Source

[`src/components/ui/toast/toast.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/toast/toast.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
