---
title: "Radio"
description: "Radio component built on Base UI, styled with sc1m tokens."
---

import { Demo as ComponentDemo } from "@/components/ui/radio/radio.demo";
import { propColumns } from "@/components/site/docs-page";
import { tokenColumns } from "@/components/site/docs-page";

<Chips items={[{ label: "Stable", tone: "success" }, { label: "Base UI" }]} />

## Live preview

<Preview>
  <ComponentDemo />
</Preview>

## Installation

<PMCommand exec="shadcn@latest add @sc1m/radio" />

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts` plus `@base-ui/react`. For the manual route — prerequisites,
`components.json`, and app-level setup — see [Installation](/docs/installation).

## Usage

```tsx lineNumbers
import { Radio } from "@/components/ui/radio";
```

## Example

The preview above, with its source. This is the demo module verbatim, so
the code and the thing it renders cannot drift apart.

<Demo>
<DemoPreview>
  <ComponentDemo />
</DemoPreview>
<DemoCode>

```tsx lineNumbers
<RadioGroup defaultValue="small" aria-label="Size">
  {['small', 'medium', 'large'].map((size) => (
    <label key={size} className="flex items-center gap-2 text-sm">
      <Radio value={size} />
      {size}
    </label>
  ))}
</RadioGroup>
```

</DemoCode>
</Demo>

## API reference
Each part forwards every prop of its Base UI counterpart; the tables list
the ones declared on the part itself. See
[Base UI's reference](https://base-ui.com/react/components/radio)
for the inherited element props.

### Radio

<SpecTable
  columns={propColumns}
  widths="1.1fr 1.3fr 0.7fr 2fr"
  rows={[
    ["value", "Value", "—", "The unique identifying value of the radio in a group."],
    ["disabled", "boolean", "—", "Whether the component should ignore user interaction."],
    ["required", "boolean", "—", "Whether the user must choose a value before submitting a form."],
    ["readOnly", "boolean", "—", "Whether the user should be unable to select the radio button."],
    ["inputRef", "React.Ref<HTMLInputElement>", "—", "A ref to access the hidden input element."],
  ]}
/>

## Accessibility

Behaviour, focus management, and ARIA wiring come from [Base UI's Radio](https://base-ui.com/react/components/radio) — see its reference for the full keyboard map.

- Keyboard focus draws a 2px ring in `--ring`, never removed — only shown on `:focus-visible`, so a pointer press does not paint one.
- Disabled parts mute their contents rather than the element, keeping the focus ring at full strength where the part stays focusable.

## Design tokens

The component reads semantic tokens only — no raw colour, radius, or
duration appears in its source. Override the token, not the component.

<SpecTable
  columns={[tokenColumns[0], tokenColumns[2]]}
  widths="1.2fr 1.8fr"
  minWidth={420}
  rows={[
    ["--accent", "Radio"],
    ["--line-strong", "Radio"],
    ["--ring", "Radio"],
    ["--surface-raised", "Radio"],
  ]}
/>

## Source

[`src/components/ui/radio/radio.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/radio/radio.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
