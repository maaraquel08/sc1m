---
title: "Radio"
description: "Radio component built on Base UI, styled with sc1m tokens."
---

import { story } from "@/components/ui/radio/radio.story";

## Preview

A representative composition. Open it in Storybook to vary the props.

<story.WithControl />

## Installation

```bash
npx shadcn@latest add @sc1m/radio
```

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts` plus `@base-ui/react`.

## Usage

```tsx
import { Radio } from "@/components/ui/radio";
```

## Exports

- `Radio`

## Source

[`src/components/ui/radio/radio.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/radio/radio.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
