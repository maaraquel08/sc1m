---
title: "Dialog"
description: "Dialog component built on Base UI, styled with sc1m tokens."
---

import { story } from "@/components/ui/dialog/dialog.story";

## Preview

A representative composition. Open it in Storybook to vary the props.

<story.WithControl />

## Installation

```bash
npx shadcn@latest add @sc1m/dialog
```

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts` plus `@base-ui/react`.

## Usage

```tsx
import {
  Dialog,
  DialogTrigger,
  DialogContent,
  DialogTitle,
  DialogDescription,
  DialogClose,
} from "@/components/ui/dialog";
```

## Exports

- `Dialog`
- `DialogTrigger`
- `DialogContent`
- `DialogTitle`
- `DialogDescription`
- `DialogClose`

## Source

[`src/components/ui/dialog/dialog.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/dialog/dialog.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
