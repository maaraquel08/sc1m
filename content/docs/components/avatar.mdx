---
title: "Avatar"
description: "Avatar component built on Base UI, styled with sc1m tokens."
---

import { Demo as ComponentDemo } from "@/components/ui/avatar/avatar.demo";
import { propColumns } from "@/components/site/docs-page";
import { tokenColumns } from "@/components/site/docs-page";

<Chips items={[{ label: "Stable", tone: "accent" }, { label: "Base UI" }]} />

## Live preview

<Preview>
  <ComponentDemo />
</Preview>

## Installation

<PMCommand exec="shadcn@latest add @sc1m/avatar" />

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts` plus `@base-ui/react`. For the manual route — prerequisites,
`components.json`, and app-level setup — see [Installation](/docs/installation).

## Usage

```tsx lineNumbers
import { Avatar, AvatarImage, AvatarFallback } from "@/components/ui/avatar";
```

## Composition

Every part is a separate export. `Avatar` owns state; the rest are
presentational and must appear in this order.

<Composition>{`
  Avatar
    AvatarFallback
`}</Composition>

## Example

The preview above, with its source. This is the demo module verbatim, so
the code and the thing it renders cannot drift apart.

<Demo>
<DemoPreview>
  <ComponentDemo />
</DemoPreview>
<DemoCode>

```tsx lineNumbers
<Avatar>
  <AvatarFallback>MR</AvatarFallback>
</Avatar>
```

</DemoCode>
</Demo>

## API reference
Each part forwards every prop of its Base UI counterpart; the tables list
the ones declared on the part itself. See
[Base UI's reference](https://base-ui.com/react/components/avatar)
for the inherited element props.

### AvatarImage

<SpecTable
  columns={propColumns}
  widths="1.1fr 1.3fr 0.7fr 2fr"
  rows={[
    ["onLoadingStatusChange", "((status: ImageLoadingStatus) => void)", "—", "Callback fired when the loading status changes."],
  ]}
/>

### AvatarFallback

<SpecTable
  columns={propColumns}
  widths="1.1fr 1.3fr 0.7fr 2fr"
  rows={[
    ["delay", "number", "0", "How long to wait before showing the fallback."],
  ]}
/>

## Accessibility

Behaviour, focus management, and ARIA wiring come from [Base UI's Avatar](https://base-ui.com/react/components/avatar) — see its reference for the full keyboard map.

## Design tokens

The component reads semantic tokens only — no raw colour, radius, or
duration appears in its source. Override the token, not the component.

<SpecTable
  columns={[tokenColumns[0], tokenColumns[2]]}
  widths="1.2fr 1.8fr"
  minWidth={420}
  rows={[
    ["--fg-muted", "Avatar"],
    ["--surface", "Avatar"],
  ]}
/>

## Source

[`src/components/ui/avatar/avatar.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/avatar/avatar.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
