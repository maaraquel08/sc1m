---
title: "Toggle"
description: "Toggle component built on Base UI, styled with sc1m tokens."
---

import { Demo as ComponentDemo } from "@/components/ui/toggle/toggle.demo";
import { propColumns } from "@/components/site/docs-page";
import { tokenColumns } from "@/components/site/docs-page";

<Chips items={[{ label: "Stable", tone: "accent" }, { label: "Base UI" }]} />

## Live preview

<Preview>
  <ComponentDemo />
</Preview>

## Installation

<PMCommand exec="shadcn@latest add @sc1m/toggle" />

This also merges the [token layer](/docs/foundations/tokens) and installs
`src/lib/cn.ts` plus `@base-ui/react`. For the manual route — prerequisites,
`components.json`, and app-level setup — see [Installation](/docs/installation).

## Usage

```tsx lineNumbers
import { Toggle } from "@/components/ui/toggle";
```

## Example

The preview above, with its source. This is the demo module verbatim, so
the code and the thing it renders cannot drift apart.

<Demo>
<DemoPreview>
  <ComponentDemo />
</DemoPreview>
<DemoCode>

```tsx lineNumbers
<Toggle aria-label="Bold">B</Toggle>
```

</DemoCode>
</Demo>

## API reference
Each part forwards every prop of its Base UI counterpart; the tables list
the ones declared on the part itself. See
[Base UI's reference](https://base-ui.com/react/components/toggle)
for the inherited element props.

### Toggle

<SpecTable
  columns={propColumns}
  widths="1.1fr 1.3fr 0.7fr 2fr"
  rows={[
    ["pressed", "boolean", "—", "Whether the toggle button is currently pressed."],
    ["defaultPressed", "boolean", "false", "Whether the toggle button is currently pressed."],
    ["disabled", "boolean", "false", "Whether the component should ignore user interaction."],
    ["onPressedChange", "((pressed: boolean, eventDetails: Toggle.ChangeEventDetails) => void)", "—", "Callback fired when the pressed state is changed."],
    ["value", "Value", "—", "A unique string that identifies the toggle when used inside a toggle group."],
  ]}
/>

## Accessibility

Behaviour, focus management, and ARIA wiring come from [Base UI's Toggle](https://base-ui.com/react/components/toggle) — see its reference for the full keyboard map.

- Keyboard focus draws a 2px ring in `--ring`, never removed — only shown on `:focus-visible`, so a pointer press does not paint one.
- Disabled parts mute their contents rather than the element, keeping the focus ring at full strength where the part stays focusable.

## Design tokens

The component reads semantic tokens only — no raw colour, radius, or
duration appears in its source. Override the token, not the component.

<SpecTable
  columns={[tokenColumns[0], tokenColumns[2]]}
  widths="1.2fr 1.8fr"
  minWidth={420}
  rows={[
    ["--accent", "Toggle"],
    ["--accent-fg", "Toggle"],
    ["--brand-radius-md", "Toggle"],
    ["--fg", "Toggle"],
    ["--ring", "Toggle"],
    ["--surface", "Toggle"],
  ]}
/>

## Source

[`src/components/ui/toggle/toggle.tsx`](https://github.com/maaraquel08/sc1m/blob/main/src/components/ui/toggle/toggle.tsx) — no `next/*` imports, so it drops into any
React 19 + Tailwind v4 app.
